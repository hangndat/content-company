# Optional: API key for dashboard (Bearer token)
# Set if orchestrator has API_KEY configured
VITE_API_KEY=your-api-key

# Optional: Langfuse UI base URL for “Mở Langfuse” on job detail (trace path appended: /trace/{traceId})
# VITE_LANGFUSE_UI_PUBLIC_URL=http://localhost:3030
